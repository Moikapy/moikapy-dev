# Admin password hash (generate with: bun run scripts/hash-password.ts <your-password>)
ADMIN_PASSWORD_HASH=

# Session signing secret (generate with: openssl rand -hex 32)
SESSION_SECRET=

# Ollama Cloud API key (get one at https://ollama.com/settings/keys)
OLLAMA_API_KEY=